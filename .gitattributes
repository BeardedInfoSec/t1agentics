# Keep shell scripts and entrypoints LF so they run under bash / WSL2 /
# Linux containers even when the repo is cloned on Windows.
*.sh text eol=lf
bin/* text eol=lf
Caddyfile text eol=lf
Dockerfile text eol=lf
*.yml text eol=lf
