# Extra dependency for the native (no-Docker) single-node runner -- run_native.py.
# pgserver ships a self-contained PostgreSQL as pip wheels for Linux, macOS, and
# Windows (no system install, no admin rights). Install alongside the backend:
#   pip install -r backend/requirements.txt -r requirements-native.txt
pgserver>=0.1.4
