# T1 Agentics Backend Requirements - Windows Compatible
# These versions work without requiring Rust or C++ compilation

# Core Framework
fastapi==0.136.3
uvicorn[standard]==0.49.0

# Data Validation
pydantic==2.13.4
pydantic-settings==2.14.1

# HTTP & Async
aiohttp==3.14.1
aiofiles==23.2.1
httpx==0.28.1
requests==2.34.2
python-multipart==0.0.32

# Configuration & Utilities
python-dotenv==1.2.2
pyyaml==6.0.3
python-dateutil==2.9.0.post0

# Validation
validators==0.35.0
dnspython==2.8.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# Database
motor==3.7.1
pymongo==4.17.0

# Redis - distributed rate limiting (falls back to in-memory if unreachable)
redis>=5.0,<6

# Authentication
passlib==1.7.4
bcrypt==4.0.1
PyJWT==2.13.0
email-validator==2.3.0

# Note: geoip2 and ipaddress removed as they require compilation
# These are only needed for the mock implementations
